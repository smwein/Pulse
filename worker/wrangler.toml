name = "pulse-proxy"
main = "src/index.ts"
compatibility_date = "2026-04-01"

# Secrets are set via `wrangler secret put` (not in this file):
#   ANTHROPIC_API_KEY
#   DEVICE_TOKEN

[observability]
enabled = true
